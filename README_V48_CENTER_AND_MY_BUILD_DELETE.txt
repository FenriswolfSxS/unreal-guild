V48 fixes

1. Centers the two-column published build grid inside the page at all desktop widths.
2. Adds a Delete button beside each entry in the My Builds dialog.
3. Build owners may always delete their own builds; leadership moderation remains unchanged.
4. Changes are additive and do not remove or overwrite D1 or R2 data.
5. CSS and JavaScript cache versions were bumped so Cloudflare/browsers load this patch.
