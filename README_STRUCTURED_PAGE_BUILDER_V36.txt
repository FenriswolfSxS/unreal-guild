V36 fixes
- Home modular content is centered in a true 1200px centered container.
- Save Page now works for Home, Events, and guide pages with the correct permission checks.
- Save button visibly shows Saving, Saved, or Save failed and preserves the page on failure.
- Main navigation is fixed at the top on every page and remains visible while scrolling.
- No manual SQL or D1 reset is required.
