SxS The Unreal V35

Fixes:
- Text fields across every modular page can be clicked and typed into normally.
- Selecting editable text no longer rebuilds the DOM or destroys the caret.
- Home modular rows now use a single full-width centered track.
- Live content changes are synchronized without rerendering the active editor.
- Selected blocks receive a gold highlight.

Deploy the entire folder over V34. No SQL changes are required.
