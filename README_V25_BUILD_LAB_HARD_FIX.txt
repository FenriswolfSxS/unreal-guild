V25 Build Lab hard fix

The signed-in profile image now has enforced inline 34x34 dimensions plus runtime-injected navigation styles, so it cannot expand to its original uploaded image size even if page-specific CSS is cached or overridden. Existing D1, R2, Build Lab, forums, roster, profile, and admin functionality is unchanged.
