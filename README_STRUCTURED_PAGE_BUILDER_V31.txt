SxS The Unreal — Structured Page Builder V31

- Removes the free-position home canvas. Home now uses the same flowing structured editor as guide pages.
- A Side by Side layout creates one horizontal section with two content areas, not two rows.
- User-facing editor language no longer calls horizontal layouts “columns.”
- Adds polished game-style toolbar and controls.
- Adds cache-busting to CSS and page-builder JavaScript on every HTML page.
- Existing home bubble content is converted into structured cards the first time the page loads and can then be saved normally.
