SxS The Unreal — Structured Page Builder V32

New text-design controls:
- Click a Heading, Text, Card, Caption, or Link Button to select it.
- Change font, size, color, alignment, bold, italic, and underline from the gaming-styled editor bar.
- Reset returns the selected block to the guild theme defaults.
- Styles are saved as structured page data and apply on every editable page.
- Image and divider blocks correctly disable text controls.

Deploy the complete project over V31. No SQL reset or migration is required.
Bindings remain: DB and R2_ASSETS.
